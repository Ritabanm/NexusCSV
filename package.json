{
  "name": "nexus-csv-converter",
  "version": "1.0.0",
  "description": "A developer tool to convert CSVs to nested JSON formats.",
  "main": "app.js",
  "scripts": {
    "dev": "vite --port 3000 --host",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
